#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int cnt;
    cin >> cnt;

    for(int i = 0 ; i < cnt ; i++) {
        int a, b;
        cin >> a >> b;
        cout << a + b << "\n";
    }

    return 0;
}